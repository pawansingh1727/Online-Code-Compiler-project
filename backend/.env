PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/online-compiler
JWT_SECRET=supersecretkey_change_in_production
# We'll use the public Judge0 instance for now. 
# If rate limited, you should get a RapidAPI key and use that endpoint instead.
JUDGE0_API_URL=https://ce.judge0.com
JUDGE0_API_KEY=
